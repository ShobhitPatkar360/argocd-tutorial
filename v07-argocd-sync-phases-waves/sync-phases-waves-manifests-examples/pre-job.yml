apiVersion: batch/v1
kind: Job
metadata:
  name: health-check-pre-job
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: HookFailed
spec:
  template:
    spec:
      containers:
        - name: health-check
          image: curlimages/curl
          command: ["/bin/sh", "-c"]
          args: ["if curl -s http://10.101.16.134:80 | grep -qi 'running'; then echo 'Your Application is up and Running'; exit 0; else echo 'Your Application is NOT accessible'; exit 1; fi"]
      restartPolicy: Never
  backoffLimit: 0
